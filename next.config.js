/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
module.exports = {
  images: {
    domains: ['mdbcdn.b-cdn.net', 'source.unsplash.com', 'api.lorem.space', 'placekitten.com']
  }
}
